type Question {
  id: ID
  question: String
}

type Query {
  questions: [Question]
  question(id: ID!): Question
}

input QuestionInput {
  body: String!
}

type Mutation {
  createQuestion(question: QuestionInput): Question
}
