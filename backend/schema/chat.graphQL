type Question {
  id: ID
  question: String
  # owner: User
}

type Query {
  questions: [Question]
  question(id: ID!): Question
}

input QuestionInput {
  question: String!
}

type Mutation {
  createQuestion(body: QuestionInput): Question
}
