type Question {
  id: ID
  question: String
  owner: ID
}

type Query {
  questions: [Question]
  question(id: ID!): Question
  questionByOwner(owner: ID!): [Question]
}

input QuestionInput {
  question: String!
  owner: ID!
}

type Mutation {
  createQuestion(body: QuestionInput): Question
}
