type Answer {
  id: ID
  answer: String
}

type Query {
  answers: [Answer]
  answer(id: ID!): Answer
}

input AnswerInput {
  answer: String!
  question: String!
}

type Mutation {
  createAnswer(body: AnswerInput): Answer
}
